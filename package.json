{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev"
  },
  "dependencies": {
    "nodemailer": "^8.0.3"
  }
}